# 15-2-1: CLAUDE.mdの育て方

## 🎯 このセクションで学ぶこと

- `CLAUDE.md` に何を書いて、何を書かないかを行ごとに判断できる。
- 曖昧な決まりを、後から確かめられる書き方に直せる。
- どうなったら1行足すのかを、自分の基準として決められる。

---

## 導入

14-1-3で作った `CLAUDE.md` に入っているのは、`/init` が書いた行から明らかに要らないものを落としたぶんだけです。このアプリの前提は、まだ依頼文の側にあります。`docs/manual-work.md` の1つ目に自分で書いたとおり、依頼のたびに書き直していました。あのとき先送りにした「何を書いて、どう育てるか」を、ここから決めます。

---

## 何を書いて、何を書かないか

分かれ目は、**コードを開けば分かることか**、そして、**来週も本当のままか**です。

| 書くもの | 書かないもの |
|:--|:--|
| コードを読んでも分からないコマンド（`./vendor/bin/sail` を頭に付ける、など） | コードを読めば分かること（フォルダの一覧・テーブルのカラム・モデルの一覧） |
| ふつうと違う書き方の決めごと（画面に出す入力チェックのメッセージは日本語にする、など） | Laravelを知っていれば分かる一般的な作法 |
| テストの回し方と、結果の見せ方 | 設計書に書いてあることの写し（代わりに「`docs/reply.md` を読んで」と1行だけ書く） |
| 触ってほしくないもの | いまだけの話（「移行作業中です」「これから作ります」） |
| このアプリだけの落とし穴（`php artisan` では動かない、など） | 「きれいなコードを書いてください」のような、当たり前のお願い |

右の列を書きたくなるのは、丁寧に書いたほうが伝わりそうに見えるからです。ただ、モデルの一覧は `app/Models/` を開けば書いてあります。書き写した分は、モデルが1つ増えた日から嘘になります。

左の列は逆です。`./vendor/bin/sail` を頭に付けるという決まりは、コードのどこを探しても書いてありません。9-1-2「Docker環境でLaravelを動かそう（Laravel Sail）」で環境を作ったときの事情です。こういうものだけが、ここに書く価値のある行です。

---

## 確かめられる書き方

書く中身が決まっても、書き方で効き目が変わります。左のように書いていたものを、右のように直します。

| こう書くと伝わらない | こう書けば伝わる |
|:--|:--|
| きれいなコードを書いてください | 入力チェックはコントローラーの `validate()` に書き、`docs/reply.md` の「入力チェックとメッセージ一覧」と同じ内容にしてください |
| ちゃんとテストしてください | 変更が終わったら `./vendor/bin/sail test` を回し、結果を報告してください |
| ファイルは整理してください | 機能のテストは `tests/Feature/` に置いてください |
| セキュリティに気をつけてください | `.env` は読まないでください。値が要るときは私に聞いてください |

右の列はどれも、**そのとおりにできたかを、後から確かめられる**形になっています。

「きれいなコードを書いてください」の場合、書けたかどうかを誰も判定できません。判定できないなら、守られたかどうかも分かりません。「機能のテストは `tests/Feature/` に置いてください」なら、フォルダを開けば1秒で分かります。14-2-1で受け入れ条件を操作の言葉で書いたときと、同じ物差しです。

ここに書いたことが強制ではないことは、14-1-3で見たとおりです。上がるのは確率で、具体的で短い行ほど従われやすくなります。確かめられる形に直すのは、その確率を上げるためです。確実に止めたいものは、この1枚に書くだけでは足りません。権限の設定で止めます（15-4-1）。

---

## 足す引き金

思いついた順に足すと、太るのはあっという間です。足したいことができたときは、ファイルを開いて自分で書きます。この教材で書き足す引き金にするのは、次のものだけです。

| 引き金 | 足す1行 |
|:--|:--|
| 同じ間違いを**2回目**にされた | その間違いを防ぐ決めごとを1行 |
| 前のセッションで打った説明を、また打っている | その説明を1行 |
| `/clear` したAIに聞いて、答えが返ってこなかった | 答えの根拠になる決めごとを1行 |

上の2つは、2026年8月時点の公式ドキュメントが挙げているものと同じです。公式はほかに、コードレビューで指摘されたとき・新しく入った人にも同じ説明が要るとき、も挙げています。1人でこのアプリを触っているあいだは出番が無いので、この教材では上の3つを使います。

上の2つでは、1回目では足しません。1回きりのことは、次に起きるとは限らないからです。2回目に起きたことは、3回目も起きます。ここが線です。

3つ目は入り方が違います。間違えられるのを待たずに、書き漏らしのほうを先に見つけます。書いたつもりの決めごとが本当に書いてあるかは、`/clear` を打ってから聞けば分かります（15-2-4）。

足すときに、もう1つ守ることがあります。**古い行を消してから、新しい行を足す**。「日本語にする」と決め直したなら、「英語のままでよい」の行はその場で消します。食い違ったまま2つ並ぶと、どちらが読まれるかは決まりません。

足す操作そのものは、VS Codeでファイルを開いて書くだけです。Claude Codeの入力欄からも開けます。`/memory` と打つと小さなメニューが出るので、`Project instructions`（このアプリの `CLAUDE.md`）を選ぶと、エディタでそのファイルが開きます（2026年8月時点）。

---

## 変えたら、効いているかを見る

書き足したり書き直したりしたら、その場で1回試します。テストの回し方を書き直したなら、次の依頼で書いたとおりのコマンドが打たれるかを見ます。

思ったとおりに動かなかったときに見るのは、次の順です。

1. その行が、後から確かめられる形になっているか
2. その行の前後に、消してよい行が並んでいないか（消せる行が多いほど、大事な1行は効きにくくなります。刈り方は15-2-2で扱います）

効かなかったときに同じことをもう一度書き足すと、ほかの行と一緒に埋もれます。増やす前に、いま書いてある1行を直してください。

> 💡 **TIP**: `/compact` で会話を要約したあとも、いちばん上の `CLAUDE.md` はディスクから読み直されます（2026年8月時点の公式ドキュメント）。会話の中だけで伝えた前提は要約で薄まりますが、この1枚に書いた分は戻ってきます（14-5-2）。

---

## 💡 Claudeが自分で取るメモ（auto memory）

`CLAUDE.md` はあなたが書くファイルです。これとは別に、Claude自身が気づいたことを書き留めていく仕組みも動いています。auto memory と呼ばれるもので、2026年8月時点では既定で有効です。

|  | `CLAUDE.md` | auto memory |
|:--|:--|:--|
| 書く人 | あなた | Claude |
| 中身 | 指示と決まり | 気づいたことや、やり方の癖 |
| 共有範囲 | リポジトリに入るので、コードと一緒にチームへ渡る | 自分のマシンの中だけ。他のマシンへは渡らない |

置き場は `~/.claude/projects/` の下です。何が書き留められているかは、`/memory` のメニューで `Open auto-memory folder` を選ぶと読めます（2026年8月時点）。中身はただのマークダウンなので、自分で消すこともできます。書く相手が違うので、混ぜて考えないでください。この教材であなたが育てるのは `CLAUDE.md` のほうです。

---

## 🏃 毎回書いていた前提を、1枚に移す

`docs/manual-work.md` に並べた5項目のうち、上から2つをこの1枚へ移します。

思いどおりに直らない・壊れたと感じたら14-5-1（直らないときの型）へ。応急は `git restore .`

```bash
# アプリのフォルダへ移動して、記録していない変更が無いかを見る
cd ~/laravel-practice/post-app-practice
git status
```

自分が触ったファイルが出てきた場合は、先にコミットしておいてください。

### Step 1: 移すものを確かめる

VS Codeで `docs/manual-work.md` を開きます。14-5-4で書き出した一覧です。この節で移すのは、上の2つです。

```markdown
- 依頼のたびに、このアプリの前提（変えないもの・入力チェックのメッセージは日本語で出すこと）を書き直した
- 実装を頼むたびに「テストも書いて、./vendor/bin/sail test で回して、通るまで直して、最後に結果を報告して」を書き添えた
```

自分のファイルの文言は、これと違っていて構いません。同じことを指していれば大丈夫です。

### Step 2: CLAUDE.md に4行足す

`CLAUDE.md` を開いて、次の4行を足します。「守ってほしい書き方」の見出しがまだ無ければ、作ってその下に置いてください。

```markdown
## 守ってほしい書き方

- 画面に出す入力チェックのメッセージは日本語にする。
- タイムラインの今ある動き（一覧・編集・削除）を壊さない。
- 実装を変えたら、テストも書いて ./vendor/bin/sail test で回し、通るまで直して、最後に結果を報告する。
- PRの本文には、テスト結果と受け入れ条件の突き合わせを書く。
```

2行目だけ、言い方を変えてあります。14-3-3の依頼文では「タイムラインの今の動き（一覧・編集・削除）は変えない」と書いていました。あれはリプライ機能を作るあいだの話で、この先タイムラインそのものに機能を足す日は来ます。「変えない」のまま毎回効く1枚に置くと、その日から嘘になります。移してよいのは、いつでも本当のままでいられる言い方に直したものだけです。1回の依頼に書いた行を1枚に移すというのは、この言い直しまで含めた作業です。

4行目は、14-4-5でPRの本文に書かせた形です。PRを出すたびに依頼文へ書いていたので、こちらへ移せます。

**書き足した `CLAUDE.md`**

<img alt="15-2-1_1.png" src="">

### Step 3: コミットする

Claude Codeを起動して、記録まで頼みます。

```bash
# アプリのフォルダで実行する
claude
```

```text
CLAUDE.md をコミットして、main に push して。
```

今回はコードを変えていないので、ブランチを切らず `main` へ記録します（14-5-4と同じです）。

続けて `/context` を打って、`Memory files` の行を見てください。読み込まれたファイルが数えられています（2026年8月時点）。

**`/context` の Memory files**

<img alt="15-2-1_2.png" src="">

ここまでで、次を確かめてください。

- [ ] `CLAUDE.md` に、上の4行が入っている
- [ ] `/context` の `Memory files` の行に、読み込まれたファイルが数えられている
- [ ] GitHubでリポジトリのページを開いて、`CLAUDE.md` に足した行が見える

3行目のテストの決まりは、15-2-3で話題ごとの別ファイルへ移します。ここではこのまま置いておいてください。

---

## ✨ まとめ

- 書くのは、コードを開いても分からないことと、来週も本当のままでいられることだけ。
- 曖昧な決まりは、後から確かめられる形に直してから置く。判定できない行は、守られたかどうかも分からない。
- 足す引き金は、同じ間違いの2回目・前のセッションで打った説明の打ち直し・`/clear` したAIが答えられなかったとき。
- 足す前に、古い行を消す。
- 1回の依頼に書いていた行を1枚へ移すときは、いつでも本当のままでいられる言い方に直す。

---
