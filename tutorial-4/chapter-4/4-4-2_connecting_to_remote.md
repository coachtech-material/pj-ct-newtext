# Tutorial 4-4-2: ローカルとリモートの接続 (git remote)

## 🎯 このセクションで学ぶこと

*   `git remote add` コマンドを使って、ローカルリポジトリに、リモートリポジトリの「住所」を登録する方法を学ぶ。
*   リモートリポジトリのニックネームとして、慣習的に `origin` が使われることを理解する。
*   `git remote -v` コマンドを使って、登録されたリモートリポジトリの情報を確認する方法を学ぶ。

---

## 導入

前のセクションで、あなたはGitHub上に、空のリモートリポジトリを作成しました。これで、あなたの手元には、2つのリポジトリが存在することになります。

1.  **ローカルリポジトリ**: あなたのコンピュータ上にある、`my-first-git` ディレクトリ。
2.  **リモートリポジトリ**: GitHubのサーバー上にある、`https://github.com/your-name/my-first-git.git` というURLのリポジトリ。

しかし、現時点では、この2つのリポジトリは、お互いの存在を全く知りません。ただ、別々に存在しているだけです。

このセクションで行うのは、あなたのローカルリポジトリに対して、「**君がこれから変更をアップロードするべき『共有の場所』は、あそこだよ**」と、リモートリポジトリの「住所」を教えてあげる作業です。この設定を行うことで、初めて、ローカルとリモートの間で、データのやり取り（`push` や `pull`）が可能になります。

---

## 詳細解説

### 🏠 リモートリポジトリの「住所」を登録する

ローカルリポジトリに、リモートリポジトリの情報を登録するためのコマンドが、`git remote add` です。

```bash
git remote add <ニックネーム> <リモートリポジトリのURL>
```

*   `git remote add`: 新しいリモートリポジトリの情報を追加する、という宣言。
*   `<ニックネーム>`: これから登録する長いURLに対する、短い「あだ名」です。毎回、長いURLを打ち込むのは大変なので、Gitでは、リモートリポジトリに、分かりやすいニックネームを付けて管理します。
*   `<リモートリポジトリのURL>`: GitHubでリモートリポジトリを作成した後に表示される、`.git` で終わるURLです。

#### `origin` というニックネーム

このニックネームには、慣習として、**`origin` (オリジン)** という名前が、ほぼ必ず使われます。「起源」や「源」といった意味です。これは、このリモートリポジトリが、このプロジェクトの「中心的な（起源となる）リポジトリ」であることを示す、世界共通の暗黙のルールのようなものです。特別な理由がない限り、`origin` という名前を使いましょう。

### 実行してみよう

それでは、実際にコマンドを実行します。ターミナルで、`my-first-git` ディレクトリの中にいることを確認してください。

1.  **URLをコピーする**: 前のセクションで作成した、GitHubのリモートリポジトリのページを開きます。「Quick setup」のページで、HTTPSのURLをコピーします。通常は、`https://github.com/あなたのユーザー名/my-first-git.git` という形式です。

    <!-- ここにスクリーンショットを挿入: GitHubのQuick setupページで、HTTPSのURLをコピーする場所 -->

2.  **`git remote add` を実行する**: ターミナルで、以下のコマンドを実行します。URLの部分は、あなたがコピーしたものに置き換えてください。

    ```bash
    # URLの部分は、あなた自身のものに置き換えてください
    git remote add origin https://github.com/your-name/my-first-git.git
    ```

    このコマンドは、成功しても、何もメッセージを返しません。しかし、水面下では、ローカルリポジトリの `.git/config` ファイルに、リモートリポジトリ `origin` の情報が、ひっそりと書き込まれています。

### ✅ 接続の確認

本当に正しくリモートリポジトリが登録されたか、確認してみましょう。`git remote` コマンドに `-v` (`--verbose` の略) オプションを付けて実行すると、登録されているリモートリポジトリのニックネームと、そのURLの一覧が表示されます。

```bash
git remote -v
```

以下のように、`origin` というニックネームに対して、2種類のURL（`fetch` と `push`）が登録されていれば、成功です。

```
origin  https://github.com/your-name/my-first-git.git (fetch)
origin  https://github.com/your-name/my-first-git.git (push)
```

*   `fetch`: リモートからデータを取得（ダウンロード）するためのURL。
*   `push`: リモートにデータを送信（アップロード）するためのURL。

通常、この2つは同じURLになります。

もし、URLを間違えて登録してしまった場合は、`git remote set-url origin <新しい正しいURL>` というコマンドで、後から修正することができます。

---

## ✨ まとめ

このセクションでは、`git remote add` コマンドを使って、ローカルリポジトリとリモートリポジトリを接続しました。

*   **目的**: ローカルリポジトリに、変更を共有するための「共有の場所（リモートリポジトリ）」の住所を教える。
*   **コマンド**: `git remote add <ニックネーム> <URL>`
*   **ニックネーム**: 慣習として、中心的なリモートリポジトリには `origin` という名前を使う。
*   **URL**: GitHubでリポジトリを作成した後に表示される、`.git` で終わるHTTPSのURL。
*   **確認**: `git remote -v` コマンドで、登録されたリモートリポジトリの情報を確認できる。

これで、あなたのローカルリポジトリは、クラウド上にあるリモートリポジトリへの「道」を知ることができました。2つのリポジトリは、今、まさに運命の赤い糸で結ばれた状態です。

次のセクションでは、いよいよ、この道を使って、ローカルリポジトリに記録したコミットの歴史を、リモートリポジトリにアップロードします。

---

## 📝 学習のポイント

- [ ] `git remote add` コマンドの役割を説明できる。
- [ ] `origin` というニックネームが、なぜ慣習的に使われるのかを理解している。
- [ ] GitHubからリモートリポジトリのURLをコピーし、`git remote add origin <URL>` を実行して、ローカルリポジトリと接続できる。
- [ ] `git remote -v` を使って、接続が正しく設定されたことを確認できる。
