ミドルウェアとは、リクエストがコントローラーに届く前、またはレスポンスがクライアントに返される前に実行される「共通処理」の仕組みです。リクエストを途中で止めたり（例：未ログインのユーザーをログイン画面に飛ばす）、レスポンスを加工したり（例：共通のヘッダーを追加する）できます。複数のミドルウェアは玉ねぎの層のように重なっており、リクエスト時は外側から内側へ、レスポンス時は内側から外側へと実行されます。認証チェックのような共通処理をミドルウェアにまとめておくことで、各コントローラーに同じコードを繰り返し書かずに済み（関心の分離）、コードの保守性が高まります。
