# Stage 1 追加教材のサムネイル

Stage 1 で新設した Tutorial 13〜16 のカリキュラム一覧用サムネイル。既存 v2〜v4 と同じ **画像生成モデルで出力する**ワークフローで、規格も同じ 1264×848（3:2）。

## 使い方

Gemini の画像生成 API で作る。既存 v2〜v4 と同じく画像生成モデルの出力で、規格も同じ 1264×848（3:2）。

```bash
export GEMINI_API_KEY=...
cd image/thumbnails/stage1
python3 scripts/generate.py            # 4枚まとめて
python3 scripts/generate.py t14        # 1枚だけ作り直す
```

`prompts/<slug>.md` の「## プロンプト本文」直下のコードブロックをそのまま API に渡し、後処理して `output/<slug>.png` に保存する。**出力は毎回変わるので、気に入らなければ同じコマンドを再実行すればよい。**

モデルは既定で `gemini-3-pro-image`。変えたい場合は `IMG_MODEL` 環境変数で指定する。

### 後処理を挟んでいる理由

生成モデルはプロンプトの厳守事項（四隅は直角・色帯は画像の最外周から）を守りきらない。既存プロンプトの冒頭にこれらが繰り返し書かれているのはそのため。`scripts/generate.py` は生成後に次を自動で行う。

1. 外周に残った白い余白を切り落とし、色帯を画像の最外周にする
2. 角が丸くなっていれば、色帯と同じ色で埋めて直角にする
3. 既存サムネと同じ 1264×848 に揃える

### 採用前に目視で確認すること

後処理で直せない項目は人が見る。

- 下部テキストが左右の色帯にかかっていないか（**特に T16 は文字数が多い**）
- **T14 / T16 の Claude ロゴが、放射状の花弁が並んだ形になっているか**（星形や太陽のような単純な形に化けやすい）
- ミニアイコンが指定どおりか（意図しない絵に置き換わることがある）
- 日本語テキストが崩れていないか

## 各サムネイル

| プロンプト | 教材 | テーマカラー | 中央ビジュアル |
|---|---|---|---|
| `prompts/t13.md` | システム設計（Tutorial13） | `#455A64` ブルーグレー | 設計図＋設計成果物のミニアイコン |
| `prompts/t14.md` | AI駆動開発（Tutorial14） | `#D97757` コーラル | Claude ロゴ（大）＋開発サイクル |
| `prompts/t15.md` | 開発の仕組み化（Tutorial15） | `#7E57C2` パープル | 設定ファイル＋自動化の部品 |
| `prompts/t16.md` | 総合アプリケーション開発（Tutorial16） | `#D4A017` ゴールド | 中央 Claude ＋ Laravel/Docker/MySQL/GitHub |

テーマカラーは既存13枚が使っていない色域から選んでいる（既存は緑 `#66BB6A` / 青 `#42A5F5` / Git橙 `#F05032` / Docker青 `#2496ED` / 薄紫 `#9FA8DA` / 濃青 `#0277BD` / 赤 `#FF5252` / ティール `#26A69A` / 橙 `#FFA726` を使用済み）。

## Tutorial16 と旧 Tutorial13 の区別

新 Tutorial16 は旧 Tutorial13 と**同名の総仕上げ教材**で、LMS 上では旧 Tutorial13 がアーカイブとして残り続けるため一覧に2つ並ぶ。

**旧 Tutorial13 のアイキャッチは変更しない方針**なので、区別は新 Tutorial16 側で作る。旧 Tutorial13 は「中央に Laravel、周囲に Docker・MySQL・GitHub」（`../v4/output/t13.png`）なので、新 Tutorial16 は**中央を Claude に差し替え、Laravel を周囲に降ろした**構図にした。

## T14 と T15 で Claude ロゴを重複させない

T14 は Claude ロゴを主役に据えている。T15 も Claude Code の教材だが、**同じロゴを2枚続けると一覧で見分けがつかない**ため、T15 には Claude ロゴを入れず設定ファイルを主役にしている。パープルの枠にコーラルのロゴを載せると配色が濁る問題も避けられる。

## LMS への反映

同期（`POST /deploy/sections/upsert`）は**アイキャッチを空で登録し、既存レコードのアイキャッチは上書きしない**。よって生成した画像は**管理画面から手動でアップロードする**。公開オペレーション（アイキャッチ設定 → 表示順 → 章の一括公開 → 教材の公開）の最初のステップに当たる。
